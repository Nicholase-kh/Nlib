#include "shape.h"

std::vector<rectangle>& circle::shape(int pixel)
{
	std::vector<rectangle> shape;
	rectangle rect;
	if (int(2*r / pixel) == 0)
	{
		rect = {c.x - r, c.y - r, }
		shape.push_back({})
		
	}
	int x = 2*r, y;
	while (x / pixel != 0 || x % pixel != 0)
	{
		
		x -= pixel
	}
}

